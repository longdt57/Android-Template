package lee.module.navigation.deeplink

internal object DeepLinkConst {
    private const val Scheme = "leegroup"
    private const val Host = "tech.leegroup"
    const val Prefix = "$Scheme://$Host/"
}